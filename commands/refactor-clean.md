Clean up the codebase by removing dead code and improving structure.

## Steps

1. Launch the `refactor-cleaner` agent
2. Detect dead code, unused imports, unused dependencies
3. Find duplicated logic
4. Remove confirmed dead code in small batches
5. Run build/tests after each batch
6. Report all changes made

Scope: $ARGUMENTS (or entire project if not specified)
