Update documentation to match current code state.

## Steps

1. Run `git diff --name-only HEAD~5` to see recent changes
2. Launch the `doc-updater` agent
3. Find related documentation for changed files
4. Update README, API docs, and inline documentation
5. Ensure examples in docs still work
6. Report what was updated

Scope: $ARGUMENTS
